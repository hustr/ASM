.386
CODE	SEGMENT USE16
		ASSUME  CS:CODE, SS:STACK
START:
		; 替换中断
		CLI;关闭中断响应
		MOV AX, 0F000H;
		MOV DS, AX; 
		MOV DX, 11E0H
		MOV AX, 2516H
		INT 21H
		STI;开启中断
		MOV AH, 4CH
		INT 21H
CODE 	ENDS
STACK	SEGMENT USE16 STACK
        DB 200 DUP(0)
STACK 	ENDS
		END  START
